name: aquaride_user
description: AquaRide – User mobile app for booking rides and live tracking.
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  mapbox_maps_flutter: ^2.3.0
  flutter_secure_storage: ^9.0.0
  web_socket_channel: ^3.0.1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
